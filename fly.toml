app = "mira"

[build]
  image = "ghcr.io/mira-reviewer/mira:latest"

[http_service]
  internal_port = 8000
  force_https = true

[[services.http_checks]]
  path = "/health"
  interval = 10000
  timeout = 2000
